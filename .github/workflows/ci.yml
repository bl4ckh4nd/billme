name: CI

on:
  push:
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'tests/**'
      - 'docker-compose.server-mode.yml'
      - '.env.server-mode.example'
      - 'package.json'
      - 'playwright.config.mjs'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/ci.yml'
      - '.github/workflows/e2e-nightly.yml'
  pull_request:
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'tests/**'
      - 'docker-compose.server-mode.yml'
      - '.env.server-mode.example'
      - 'package.json'
      - 'playwright.config.mjs'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/ci.yml'
      - '.github/workflows/e2e-nightly.yml'

jobs:
  validate:
    name: Validate (Typecheck, Tests, Build)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.1.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install native runtime libraries
        run: sudo apt-get update && sudo apt-get install -y libsecret-1-0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare legacy SQLite test binding
        run: pnpm ci:prepare-sqlite-tests

      - name: Shared desktop data tests
        run: pnpm -C packages/desktop-data test

      - name: Shared desktop renderer tests
        run: pnpm -C packages/desktop-renderer test

      - name: Desktop typecheck
        run: pnpm -C apps/desktop typecheck

      - name: Desktop tests
        run: pnpm -C apps/desktop test --run

      - name: Desktop e-invoice conformance tests
        run: pnpm -C apps/desktop test:einvoice

      - name: Desktop build
        run: pnpm -C apps/desktop build

      - name: Pro desktop typecheck
        run: pnpm -C apps/pro-desktop typecheck

      - name: Pro desktop tests
        run: pnpm -C apps/pro-desktop test --run

      - name: Pro desktop e-invoice conformance tests
        run: pnpm -C apps/pro-desktop test:einvoice

      - name: Pro desktop build
        run: pnpm -C apps/pro-desktop build

      - name: Offer portal build
        run: pnpm -C apps/offer-portal build

      - name: E2E smoke (desktop + pro)
        run: pnpm test:e2e:smoke

  server-packages:
    name: Server packages (Typecheck, Tests)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.1.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare legacy SQLite test binding
        run: pnpm ci:prepare-sqlite-tests

      - name: Server core typecheck
        run: pnpm -C packages/server-core typecheck

      - name: Server core tests
        run: pnpm -C packages/server-core test

      - name: Server data typecheck
        run: pnpm -C packages/server-data typecheck

      - name: Server data tests
        run: pnpm -C packages/server-data test

      - name: Server CLI typecheck
        run: pnpm -C packages/server-cli typecheck

      - name: Server API typecheck
        run: pnpm -C apps/server-api typecheck

      - name: Server API tests
        run: pnpm -C apps/server-api test

      - name: Server worker typecheck
        run: pnpm -C apps/server-worker typecheck

      - name: Server worker tests
        run: pnpm -C apps/server-worker test

      - name: Offer portal tests
        run: pnpm -C apps/offer-portal test

      - name: Demo tests
        run: pnpm -C apps/demo test

      - name: E2E artifact hygiene
        run: pnpm test:e2e:artifact-hygiene

  e2e-server-smoke:
    name: E2E smoke (server mode)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.1.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare legacy SQLite test binding
        run: pnpm ci:prepare-sqlite-tests

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E smoke (server mode)
        run: pnpm test:e2e:server:smoke

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-server-smoke
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload server-mode diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: server-mode-diagnostics-smoke
          path: |
            test-results/server-mode
            !test-results/server-mode/**/compose.env
            !test-results/server-mode/**/runtime-state.json
          if-no-files-found: ignore

  e2e-server-pro-full:
    name: Full E2E (server mode, Pro accounting)
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.1.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare legacy SQLite test binding
        run: pnpm ci:prepare-sqlite-tests

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run full server-mode Pro E2E suite
        run: pnpm test:e2e:server:full:pro

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-server-pro-full
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload server-mode diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: server-mode-diagnostics-pro-full
          path: |
            test-results/server-mode
            !test-results/server-mode/**/compose.env
            !test-results/server-mode/**/runtime-state.json
          if-no-files-found: ignore
