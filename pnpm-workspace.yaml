packages:
  - "apps/*"
  - "packages/*"

# pnpm 10's authoritative build-script allowlist lives in the root
# package.json. Keep this workspace-level mirror for older tooling and for
# documenting the native artifacts required by Electron and the test runner.
onlyBuiltDependencies:
  - electron
  - better-sqlite3
  - keytar
